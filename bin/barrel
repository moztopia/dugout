#!/bin/sh
set -eu
TTY_FLAG=""
[ -t 0 ] && [ -t 1 ] && TTY_FLAG="-t"
exec docker run --rm --init -i $TTY_FLAG \
  -v "$(pwd):/workspace" \
  -w /workspace \
  ghcr.io/moztopia/dugout-tools:latest \
  barrel "$@"
